<!DOCTYPE html>
<html lang="en">

  <head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>ANTI Fake News</title>

    <link href="css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <!--Bootsrap 4 CDN-->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
      
      
      <!--Fontawesome CDN-->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css" integrity="sha384-mzrmE5qonljUremFsqc01SB46JvROS7bZs3IO2EmfFsd15uHvIt+Y8vEf7N7fWAU" crossorigin="anonymous">
    <!-- Custom styles for this template -->
    <link href="css/blog-home.css" rel="stylesheet">
    <style>

    body {
      padding-top: 54px;
    }

    @media (min-width: 992px) {
      body {
        padding-top: 56px;
      }
    }

    </style>

  </head>

  <body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
      <div class="container">
        <a class="navbar-brand" href="#">Instagram Clone</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
         <div class="collapse navbar-collapse" id="navbarResponsive">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item ">
              <a class="nav-link" href="http://localhost/search_noticias"><span class="fa fa-search"></span> Pesquisar
              </a>
            </li>
            <li class="nav-item active">
               <a class="nav-link" href="http://localhost/ranking"><span class="fa fa-mountain"></span> Ranking</a>
            </li>
            <li class="nav-item">
               <a class="nav-link" href="http://localhost/logout"><span class="fa fa-sign-out-alt"></span> Sair</a>
            </li>
          </ul>
        </div>
        </div>
      </div>
    </nav>
    <!-- Page Content -->
    <div class="container">

      <div class="row">

        <!-- Blog Entries Column -->
        <div class="col-md-8">

          <h1 class="my-4">Ranking
            <small>Top 10 Fake News</small>
          </h1>

          <div id="container_timeline">
          </div>


          <!-- Pagination -->
          <!-- 
          <ul class="pagination justify-content-center mb-4">
            <li class="page-item">
              <a class="page-link" href="#">&larr; Antigos</a>
            </li>
            <li class="page-item disabled">
              <a class="page-link" href="#">Novos &rarr;</a>
            </li>
          </ul>
          -->

        </div>

<div class="col-md-4">
          <!-- Categories Widget -->
          <div class="card my-4">
            <h5 class="card-header">Meu Jornal</h5>
            <div class="card-body ">
              <div class="row" id="profile">
               
              </div>
            </div>
          </div>
        </div>

      </div>
      <!-- /.row -->

    </div>
    <!-- /.container -->

    <!-- Footer -->
    <footer class="py-5 bg-dark">
      <div class="container">
        <p class="m-0 text-center text-white">Copyright &copy; Anny Walker 2018</p>
      </div>
      <!-- /.container -->
    </footer>

    <script type="text/javascript">
    
     
      var url_string = window.location.href;
      var url = new URL(url_string);
      var c = url.searchParams.get("q");
      
      var user = '<%= (user.username) %>';
      
      alert(user);

      $(document).ready(function(){

        function carrega_perfil(){
          //criar xmlhttrequest
          var xhr2 = new XMLHttpRequest();

          //monta requisicao
          xhr2.open("GET","http://localhost:8080/api_get_profile/"+user);
          xhr2.onload = function(){
            if (xhr2.status === 200){

              var data = $.parseJSON(xhr2.responseText);

               $('#profile').append(
                    '<div class="col-sm-12 col-md-12">'+
                        '<h4>'+
                            '<h3 id="username" class="username">'+data.username+
                        '</h3></h4>'+
                          '<p>'+
                        "NOME: "+data.name+
                        '</p>'+
                        '<p>'+
                        "CNPJ: "+data.cnpj+
                        '</p>'+
                    '</div>'
              );
            } 
        }

         xhr2.send();

      }



      carrega_perfil();

              function doRequestInsertFakeNewsInto( xhr, link, user) {
            return new Promise( function(resolve, reject) {
              var formData_ = new FormData();
              formData_.append("link", link);
              formData_.append("user", user);
              xhr.open('POST', 'http://localhost:8080/insert_fake_news_into');
              xhr.onreadystatechange=function(){
                  if (xhr.readyState == 4 && xhr.status == 200){
                      resolve(xhr.responseText);
                    }
              }
            
              xhr.send(formData_);
            });
        }

        function doRequestInsertTrueNewsInto( xhr, link, user) {
            return new Promise( function(resolve, reject) {
              var formData_ = new FormData();
              formData_.append("link", link);
              formData_.append("user", user);
              xhr.open('POST', 'http://localhost:8080/insert_true_news_into');
              xhr.onreadystatechange=function(){
                  if (xhr.readyState == 4 && xhr.status == 200){
                      resolve(xhr.responseText);
                    }
              }
            
              xhr.send(formData_);
            });
        }

        function doRequest(i, xhr, id) {
            return new Promise( function(resolve, reject) {
              
              var formData_ = new FormData();
              formData_.append("id", id);
              xhr.open('POST', 'http://localhost:8080/api_get_noticia_by_id');
             
              xhr.onreadystatechange=function(){
                  if (xhr.readyState == 4 && xhr.status == 200)
                      resolve({"responseText" : xhr.responseText, "i" : i, "xhr": xhr});
              }
            
              xhr.send(formData_);
            });
        }

      var clicked_fake_news = false;

        function carrega_top() {
          var xhr = new XMLHttpRequest();

          xhr.open('GET', 'http://localhost:8080/api_get_top_fake_news');

          xhr.onload = function(){
            if (xhr.status === 200 ){
              noticias = JSON.parse(xhr.responseText);
              for (var i = 0 ; i < noticias.length; i++){
              doRequest(i, new XMLHttpRequest(), noticias[i]._id).then(function (response){
            

                  var fake_news;
                  var true_news;

                  var noticia = JSON.parse(response.responseText);
       

                  
                  $('#container_timeline').append(
                    '<div class="card mb-4">'+
                        '<div class="card-header"><a href="'+noticia.link_news+'"><h2>'+noticia.link_news+'</h2></a></div>'+

                         '<div class="comentarios" id="comentarios_'+noticia.link_news+'"></div>'+
                     '<div class="card-body">'+
                          '<input type="text" class="form-control input_comentario" id="comentario_news_'+noticia.link_news+'" placeholder="Adicionar comentário...">'+
                          '<button class="btn btn-default btn_postagem" value="'+noticia.link_news+'" type="button">Comentar</button>'+
                      '</div>'+

                        '<div class="card-body">'+
                          '<button class="btn btn-danger btn_fake_news" value="'+noticia.link_news+'" id="'+noticia.link_news+'_fake"  type="button">'+noticia.fake_news.length+' Fake News</button>'+
                          '<button class="btn btn-success btn_true_news" value="'+noticia.link_news+'" id="'+noticia.link_news+'_true" type="button">'+noticia.true_news.length+' True News</button>'+
                        '</div>'+
                      '</div>'
                      );

                    if (noticia.comentarios != undefined){
                        var comentarios = noticia.comentarios;

                        for (var j = 0 ; j < comentarios.length ; j++){

                          var new_html = "<b>"+comentarios[j].user + "</b> disse: " +comentarios[j].comentario;

                          var new_elem = document.createElement('div');

                          new_elem.classList.add("card-body");

                          new_elem.innerHTML += ' ' + new_html;

                          document.getElementById("comentarios_"+noticia.link_news).appendChild(new_elem);
                        }

                      } 
                  
                                $('.btn_postagem').click(function(){
                if (!clicked_fake_news){
                  clicked_fake_news = true;
                
                var id = this.value;
                var id_input_comentario = "comentario_news_"+id;
                var comentario =  document.getElementById(id_input_comentario);
              


                var xhr = new XMLHttpRequest();

                xhr.open('PUT', 'http://localhost:8080/api_comentario/');
                xhr.setRequestHeader('content-type', 'application/json');

                xhr.onload = function(){
                  if (response.xhr.status === 200 ){
                    window.location = window.location.href;
                    clicked_fake_news = false;
                  }
                }

                xhr.send(JSON.stringify({comentario: comentario.value, user: user, link_news: id}));
                }
              });
              
               $('.btn_fake_news').click(function(){
                  var id = this.value;
                  if (!clicked_fake_news){
                    clicked_fake_news = true;
                    doRequestInsertFakeNewsInto(new XMLHttpRequest(), noticia.link_news, user).then(function (response){

                      
                      var container = document.getElementById(id+"_fake");
                      var content = container.innerHTML;
                    

                      var fake_news = content.replace(/[A-Za-z$-]/g, "");
                      var add = 0;
                      switch(JSON.parse(response).ok){
                        case 0:
                          add = 1;
                          break;
                        case 1:
                          add = -1;
                          break;
                        case 2:
                          add = 1;
                          break;
                      }

                      container.innerHTML= parseInt(fake_news)+add+" Fake News"; 
                      clicked_fake_news = false;
                    
                      

                    });
                 }
                
                });
                $('.btn_true_news').click(function(){
                var id = this.value;
                  if (!clicked_fake_news){
                    clicked_fake_news = true;
                    doRequestInsertTrueNewsInto(new XMLHttpRequest(), noticia.link_news, user).then(function (response){
                      
                      
                      var container = document.getElementById(id+"_true");
                      var content = container.innerHTML;
                      
                      var xhr__ = new XMLHttpRequest();

                      
                      var true_news = content.replace(/[A-Za-z$-]/g, "");
                      var add = 0;
                      switch(JSON.parse(response).ok){
                        case 0:
                          add = 1;
                          break;
                        case 1:
                          add = -1;
                          break;
                        case 2:
                          add = 1;
                          break;
                      }

                      container.innerHTML= parseInt(true_news)+add+" Fake News"; 
                      clicked_fake_news = false;
                    
                    });

                 }
              });

                  });
                
              }

            }
          }

          xhr.send();
        }


        carrega_top();

      });
    </script>
    <!-- Bootstrap core JavaScript -->
    <script src="vendor/jquery/jquery.min.js"></script>
    <script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

  </body>

</html>
